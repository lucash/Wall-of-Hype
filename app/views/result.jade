extends layouts/layout
block content
    div.well
      h1.centered total votes: #{poll.totalVotes}
      h3.centered Die Abstimmung läuft bis zum 20.12.15 - Die Top 15 betritt die Wall of Hype. Ein WoH GOTY Voting kommt 2016!
      table#resultTable
        thead
          tr
            td Name
            td # of votes
            td %
        tbody
          each option in poll.options
            tr
              - var v = Math.round((option.votes*100)/poll.totalVotes)
              td #{option.title}
              td #{option.votes}
              td #{v}


      script.
        $(document).ready(function(){
          $('#resultTable').DataTable({
            "order": [[1, "desc"]],
             "iDisplayLength": 200,
             paging: false
          });
          $("#nav-voting").addClass("active");
        });
